<%= form_for(resource, as: resource_name, url: registration_path(resource_name), :html => {class: 'form-signin'}) do |f| %>
  <h2 class="form-signin-heading">Créer votre compte</h2>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :email, class: 'sr-only' %><br />
    <%= f.email_field :email, autofocus: true, placeholder: 'Votre adresse email', class: 'form-control' %>
  </div>

  <div class="field">
    <%= f.label :password, class: 'sr-only' %>
    <%= f.password_field :password, autocomplete: "off", placeholder: 'Mot de passe *', class: 'form-control' %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation, class: 'sr-only' %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: 'Répétez votre mot de passe', class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit "Créer mon compte", class: "btn btn-primary btn-lg btn-block" %>
  </div>
  * <% if @minimum_password_length %>
  <em>(<%= @minimum_password_length %> caractères minimum)</em>
  <% end %>
<% end %>

<% #render "users/shared/links" %>
